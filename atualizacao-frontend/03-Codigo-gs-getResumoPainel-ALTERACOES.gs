/**
 * Em getResumoPainel, adicione as buscas (junto das outras const ... = get...):
 *
 *   const pessoas = getPessoas(token);
 *   const coordenacoes = getCoordenacoes(token);
 *
 * No return { ... }, inclua:
 *
 *   pessoas: pessoas,
 *   coordenacoes: coordenacoes,
 */
